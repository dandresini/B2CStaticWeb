@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo($"/.auth/login/b2c?post_login_redirect_uri={Uri.EscapeDataString(Navigation.Uri)}");
    }
}
